spring:
  security:
    oauth2:
      resourceserver:
        jwt:
          issuer-uri: ${AZURE_ISSUER_URI}
          audiences: api://${AZURE_API_CLIENT_ID}

agrotrack:
  kafka:
    replicas: ${KAFKA_REPLICAS:3}
